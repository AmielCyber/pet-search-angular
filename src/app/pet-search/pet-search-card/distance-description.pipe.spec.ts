import { DistanceDescriptionPipe } from './distance-description.pipe';

describe('DistanceDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
